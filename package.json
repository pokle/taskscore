{
  "name": "taskscore",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "author": "Tushar Pokle",
  "description": "Hanggliding/paragliding competition track log analysis",
  "keywords": ["hanggliding", "paragliding", "igc", "competition", "scoring", "flight-analysis"],
  "homepage": "https://taskscore.shonky.info",
  "repository": {
    "type": "git",
    "url": "https://github.com/pokle/taskscore.git"
  },
  "bugs": {
    "url": "https://github.com/pokle/taskscore/issues"
  },
  "type": "module",
  "scripts": {
    "dev": "[ -d node_modules ] || bun install && bun run --filter '@taskscore/frontend' dev",
    "build": "bun run --filter '@taskscore/frontend' build",
    "deploy": "bun run --filter '@taskscore/frontend' deploy",
    "deploy:worker": "bun run --filter airscore-api deploy",
    "deploy:all": "bun run deploy && bun run deploy:worker",
    "deployments": "wrangler pages deployment list --project-name=taskscore --json | jq .",
    "typecheck": "tsc --noEmit",
    "typecheck:engine": "tsc --noEmit -p web/engine/tsconfig.json",
    "typecheck:worker": "bun run --filter airscore-api typecheck",
    "typecheck:all": "bun run typecheck && bun run typecheck:engine && bun run typecheck:worker",
    "test": "bun test",
    "detect-events": "tsx web/engine/cli/detect-events.ts",
    "get-xcontest-task": "tsx web/engine/cli/get-xcontest-task.ts"
  },
  "workspaces": [
    "web/engine",
    "web/frontend",
    "web/workers/*"
  ],
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260305.1",
    "@types/bun": "^1.3.10",
    "@types/node": "^25.3.5",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.71.0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fontsource/atkinson-hyperlegible-next": "^5.2.7",
    "mapbox-gl": "^3.19.1",
    "threebox-plugin": "^2.2.7"
  }
}